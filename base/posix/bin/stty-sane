#!/bin/bash

reset
stty sane -ixon start undef stop undef erase '^?' susp '^Z' swtch undef

test -n "$DISPLAY" && {
    setxkbmap
    xmodmap ~/.Xmodmap
    # numpad not working?
    for ((i=1; i<=31;++i)); do
        xset -led $i
    done
}
